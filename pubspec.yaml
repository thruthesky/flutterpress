name: flutterpress
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  hive: ^1.4.4
  hive_flutter: ^0.3.1

  cupertino_icons: ^0.1.3
  dio: ^3.0.10
  get: ^3.4.6
  after_layout: ^1.0.7+2
  devicelocale: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_driver:
    sdk: flutter
  test: any

flutter:

  uses-material-design: true

